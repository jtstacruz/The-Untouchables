#PATH TO DATABASE USING COMMAND PROMPT
	cd c:\\xampp\mysql\\bin
	mysql.exe -u root

#create database named "Reviewer" in XAMPP
	use reviewer

SQL SCRIPT

CREATE TABLE REVIEWSITES(REVIEWSITES_ID INTEGER PRIMARY KEY NOT NULL, REVIEWSITES_NAME VARCHAR(25) NOT NULL);

INSERT INTO REVIEWSITES VALUES(001, 'Agoda');
INSERT INTO REVIEWSITES VALUES(002, 'Booking');
INSERT INTO REVIEWSITES VALUES(003, 'TripAdvisor');

CREATE TABLE CUSTOMER (CSTMR_ID INTEGER NOT NULL PRIMARY KEY, REVIEWSITES_ID INTEGER NOT NULL, CSTMR_REVIEWER VARCHAR(25), CSTMR_REVIEWTITLE VARCHAR(100), CSTMR_REVIEW VARCHAR(5000), CSTMR_RATINGDATE VARCHAR(100), CSTMR_RATING VARCHAR(10), FOREIGN KEY(CSTMR_ID) REFERENCES REVIEWSITES(REVIEWSITES_ID));

select *from reviewsites;
select *from customer;
describe reviewsites;
describe customer;


#open cmd to import data from excel to database
#to show the number of imported columns and rows to MySQL
	cd desktop
	python pytest.py

#to show the list of imported data from excel into MySQL
	select *from customers;
